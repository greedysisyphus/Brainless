# Studio / Classic：產品策略備註（階段 6）

- **Classic**：已於 2026-08 下線。全站固定 Club；`localStorage app-theme` 會遷移為 `club`。程式裡 `isStudio` 仍為 true，僅為相容既有 Club 視圖分支。
- **Playground**：仍定位為內部元件與 hash 實驗區（含 Music 導向 `#music`、UI 樣板 `#studio-ui`）；若未來要對一般使用者開放，需在導覽與完成度上另訂規範。
