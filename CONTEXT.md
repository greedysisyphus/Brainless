# Brainless

店內營運工具（排班、點豆、收銀等）的共用語彙。

## Language

### Themes

**Club**：
目前唯一介面主題。暖紙底、墨色字、珊瑚橘強調；內容走 Cw* 元件契約。
_Avoid_: Studio light、奶油主題、淺色 Studio、再加第二套主題

**Studio**：
程式裡 `isStudio`／`DualThemePage` 的 `studio` prop 仍指 Club 視圖，不是獨立主題。
_Avoid_: 把 Club 直接叫 Studio（對使用者）

**Classic**：
已下線的深紫玻璃介面。
_Avoid_: 再新增 Classic 分支或主題切換器

### 點豆／重量換算

**重量換算**：
依分店重量參數（銀袋／盒子／每包克數），把總重估算成豆包數的彈窗工具。
_Avoid_: 計算器（單獨使用時易與其他工具混淆）、重量設定（那是其中一個區塊）

**重量換算捷徑**：
在咖啡豆盤點表操作過程中，用來快速開啟重量換算的入口（不必回到頁面上方工具列）。Club 採右下角小圓圖示鈕：白底、珊瑚計算機圖示、細邊與輕陰影；重量換算彈窗開啟時隱藏。尺寸：手機 44×44／邊距 16；iPad 48×48／邊距 20；電腦 44×44／邊距 24。
_Avoid_: FAB（實作用語）、浮動按鈕（未指明用途時）、帶文字的膠囊鈕、珊瑚實心大鈕

**關閉重量換算**：
Club 以右上角關閉鈕，或點擊彈窗外遮罩來關閉；不另設底部「完成」鈕。
_Avoid_: 僅能按 X、底部完成鈕（作此決策時）

**計算優先**：
Club 重量換算彈窗的資訊層級：開啟後先呈現總重輸入與估算包數；分店、計算模式、重量設定屬次要設定。
_Avoid_: 設定優先、長表單直鋪（作 Club 目標時）

**設定摘要列**：
Club 重量換算中收納分店與各店重量參數（銀袋重、盒重、每包克數）的摘要列；點擊後展開。容器類型不放這裡。
_Avoid_: 設定分頁、多段手風琴（作此決策時）、在設定裡切換銀袋／盒計算方式

**容器類型**：
重量換算用來區分銀袋或盒子（IKEA／MUJI）的計算容器；與「容量」無關。兩種選項互斥，一次只能選一種。Club 以兩顆點選芯片呈現（選中可帶小勾），放在彈窗外層計算區方便頻繁切換；設定區只留分店與重量參數。
_Avoid_: 容量類型、計算模式（若易與其他模式混淆）、容器下拉選單（Club 目標 UI）、只藏在設定裡

**來源分店優先**：
從某分店的咖啡豆盤點表開啟重量換算時，計算所用分店應先對齊該盤點表分店。每次重新打開都再次對齊；設定展開內仍可用三顆芯片暫時改分店。
_Avoid_: 同頁記住上次重量換算分店、設定內鎖死分店

### 貨物叫貨

**貨物叫貨**：
依各店輸入的現有貨量對照最低庫存，決定要不要向三重叫貨，並產出可複製貼上的叫貨文字。介面採 Club；以手機與 iPad 觸控為主（清單式盤點，非桌機試算表）。手機列以現有貨量為主、需叫貨時再露出叫貨量；iPad 列可同時露出現有、最低、叫貨量與狀態。
_Avoid_: 貨物盤點表（若指最終產出）、耗材盤點（未特指時）、咖啡豆盤點（那是點豆）、Studio／Classic 版貨物叫貨（未另定時）、桌機試算表主介面、手機一列塞滿所有欄位

**現有貨量**：
盤點當下該品項手上有的數量；用來判斷需叫貨或不需叫貨。可填點數（如 0.5）或份數（如 1/2）。
_Avoid_: 庫存（單獨使用時易與咖啡豆庫存混淆）、存量設定

**需叫貨**：
現有貨量已填且低於最低庫存時的狀態（可手動改為不需叫貨）。
_Avoid_: 建議叫、下次（作狀態標籤時）、要叫

**不需叫貨**：
現有貨量已填且不低於最低庫存，或手動改為本次不叫。
_Avoid_: 下次、建議叫（作狀態標籤時）

**最低庫存**：
品項可接受的最低存量；現有貨量已填且嚴格低於它時，標為需叫貨。尷尬時可手動改成需叫貨或不需叫貨。
_Avoid_: 安全庫存（未特指時）、補貨點（未特指時）、低於或等於就叫（未另定時）

**預設叫貨量**：
品項被建議叫貨時帶入的固定數量；輸出前可手動改。
_Avoid_: 補貨量（若指自動補到目標）、建議補滿量

**叫貨文字**：
可 copy-paste 的叫貨清單，格式如「三重➡️桃機D7」加上品項與數量列；只含確定要叫的品項。
_Avoid_: 報表、匯出圖片、盤點表輸出（若指這段文字）

**品項單位**：
每個貨物品項各自一個單位標籤（箱、盒、包、條等）；現有貨量、最低庫存、預設叫貨量都用該單位計數。
_Avoid_: 雙單位換算、全店統一單位

**分數數量**：
現有貨量可填點數或分數：`0.5`＝`1/2`，`1.5`＝`1 1/2`（也可用 `3/2`）。叫貨文字輸出採 `1/2`、`1 1/2` 這類寫法。不使用 `1/1/2`。
_Avoid_: `1/1/2`、只准整數、另建「半箱」獨立品項（若只是為了半數）

**叫貨店名**：
出現在叫貨文字第一行「三重➡️{叫貨店名}」的目的地名稱；每店可自訂。預設：中央店＝桃機一、D7 店＝桃機D7、D13 店＝桃機D13。來源固定為「三重」。
_Avoid_: 直接用分店內部名（中央店／D7 店／D13 店）當叫貨文字店名（未自訂時）、可改的來源名稱（未另定時）

**店別品項清單**：
每間店各自維護完整貨物品項（名稱、單位、最低庫存、預設叫貨量、是否允許分數數量、是否停用）；不與其他店共用主檔。清單順序即叫貨文字順序。叫貨文字一律一品項一行；特殊措辭靠品名／單位文字本身，不做組合列或變體組。
_Avoid_: 全店共用品項主檔、只覆寫最低庫存的共用清單、組合列／變體組資料模型、叫貨時另排輸出順序

**停用品項**：
暫時不使用但仍保留設定的貨物品項；停用後不出現在盤點與叫貨，之後可再開。
_Avoid_: 只能刪除、封存（未特指時）

**品項設定層**：
由齒輪開啟的設定介面；清單列預設收合，點開才編輯細節，以降低視覺密度。不另開獨立路由。
_Avoid_: 獨立設定頁、列上長按改設定（未另定時）、設定列一次攤開全部欄位

**未盤點**：
現有貨量空白表示還沒盤到這項；不視為 0，也不自動標為需叫貨。盤點清單預設顯示全部品項，可篩選未盤點／需叫貨／不需叫貨。
_Avoid_: 空白當 0、沒填就當成缺貨、預設只顯示未盤點

**叫貨快照**：
該店目前這一版已產好的叫貨文字（只含確定要叫的品項），供複製貼上；之後 Shortcut 也取同一份、不當場重算。在按下「複製叫貨文字」時更新。複製按鈕固定在畫面底部，並可顯示本次要叫項數。複製前先唯讀預覽確認，再複製；預覽不提供直接改字（要改回清單調）。若沒有要叫的品項，不複製並提示本次無需叫貨。
_Avoid_: 即時重算結果、一改數量就覆寫快照、另設儲存快照按鈕（未另定時）、複製鈕只放頂部、預覽內直接編輯叫貨文字（未另定時）、無品項時仍複製標題或「無需叫貨」全文

**本機優先**：
套用在現有貨量與叫貨量覆寫：編輯中或尚未成功上傳前，不以雲端覆蓋本機；雲端較新時只提示（保留本機／合併／用雲端），並顯示各版本時間。本機草稿另存。品項設定（名稱、單位、最低庫存、預設叫貨量等）仍即時雲端同步。
_Avoid_: 後寫入覆蓋、編輯中靜默套用遠端、連品項設定也進入本機優先（未另定時）

**版本時間**：
本機與雲端各自標示該版本的日期時間（例如本機最後編輯、雲端最後更新），讓弱網店在選擇保留哪邊時看得清楚。
_Avoid_: 只說「有衝突」卻不標時間

### 班表

**班表**：
三家店（桃機一店、桃機D7、桃機D13）的月排班；資料來源是班表轉換器對紙本班表拍照後產出的 JSON 匯出檔，本站只讀不改格子。
_Avoid_: 排班（本站不負責排班，只呈現）、Playground 的舊「班表管理」（那是另一套貼上式匯入）

**匯出檔**：
Brainless-SimpleKaffa-Shifts-Convertor 的 `.json` 或 `.flat.json`，兩種都吃。店別與月份由檔案本身決定，匯入時不必手選：完整版看 `store.code`／`period`，flat 版從 `employeeId` 的店代碼前綴與日期範圍推回來。flat 少了國定假日與轉檔警告（沒排班的日子本來就畫成空白，不算損失），所以有完整版時優先用完整版；用 flat 覆蓋已經是完整版的同店同月時要先警告。
_Avoid_: 手動選店別／月份、把匯出檔內容當可編輯來源、讓 flat 靜靜地蓋掉完整版

**資料契約**：
轉換器那邊維護的規格。要對齊的重點：`schema_version` 主版本不同就擋下（結構可能變了）；`kind` 有 `WORK`／`LEAVE`／`EMPTY`／`UNKNOWN` 四種，`UNKNOWN` 是連字都認不出來，**不能掉進上班分支被畫成正常的班**；`hours` 可能是 `null`，不能當 0 也不能變 NaN；時間以格子自己的 ISO 為準（跨夜的 `end` 日期是隔天），班別表只是那個月的通則；崗位代碼菜口是 `VEG_STATION`。
_Avoid_: 只認得 WORK/LEAVE、把 UNKNOWN 當成一個班、用班別表的通則時間蓋掉格子自己的時間、把 `Number('')` 當成有效版本號

**代碼表**：
匯出檔自己帶的 `shift_types`／`leave_types`／`positions` 三張表，是班別、假別與崗位的權威來源。店長可以在轉換器介面新增班別，所以本站不寫死清單；已知代碼只提供樣式與後備標籤，未知代碼照樣長出欄位與色塊。
_Avoid_: 在前端寫死班別／假別清單、遇到沒看過的代碼就丟掉或顯示空白

**月份文件**：
Firestore `shiftMonths` 裡「一店一月一份」的文件，ID 為 `YYYY-MM_storeCode`。同店同月再匯入即整份覆蓋。
_Avoid_: 一份文件塞多店、逐格增量合併（未另定時）

**人員鍵**：
用姓名當跨店跨月的同一人識別。匯出檔的 `employee_id` 只用來把格子對回同一份檔案裡的人：舊版是 `emp01_小明`（嵌列號，跨月會變），新版是 `D7_Ann`（跨月穩定，但跨店不通用，同一個人在 `central` 與 `D13` 會是兩個 id）。兩種格式都要能匯入。轉換器判讀不出姓名時會是「未命名N」，標為姓名待確認、不與他店合併。
_Avoid_: 用 employee_id 當人員識別、只支援其中一種 id 格式、把「未命名N」直接當成一個人

**佔位列**：
匯出檔裡不是真人的列，例如 D13 的「支援」列。完整班表照原樣顯示，但不列入出勤名單與統計。
_Avoid_: 把「支援」列當成一位同事、在今天誰上班裡列出佔位列

**支援班**：
在別家店上班的班，`isSupport` 為真的人當天不在本店，算本店人力時要排除。紙本記號 `T3`＝去 D13、`D7`＝去 D7；`3晚`／`7早` 則連班別一起指定。只寫 `T3`／`D7` 的**本來就沒有班別**，`start`／`end`／`hours` 是 null——那是紙本沒寫，不是資料漏了。時間一律換成目的店的班別時間。
_Avoid_: 支援算在原店、T3 只當成 D13（其他店也會有支援）、用來源店的班別時間、把 null 時間當成資料缺漏

**支援班配對**：
兩邊紙本各缺一半：來源店寫「誰」去支援（T3／D7）但沒寫班別，目的店的「支援」列寫「什麼班」但沒寫是誰。配對規則由強到弱：目的店的 `visitor` 直接照名字對（最強證據）→ 紙本寫明班別的（3早／7晚）認領班別相同的空位 → 手動指定 → 剩下剛好一人一空位才自動配 → 其餘留白並標出狀態。狀態有：已對上、同一天多人待指定、目的店沒寫、沒人認領、目的店未匯入。來源標成 `visitor`／`sheet`／`manual`／`auto` 四種，畫面要分得出來。目的店寫了 `visitor` 但那個人所屬店還沒匯入時，至少要把名字講出來，不要只說「沒有店寫是誰去」。手動配對存在 Firestore `shiftSupportLinks`，重新匯入班表不會清掉。
_Avoid_: 多人多空位時亂配、配不到就隨便挑一個班、把配對結果存在月份文件裡（重匯就沒了）、**把寫明班別的支援排除在配對之外**（目的店那一格會被誤報成沒人認領）

**判不出班別的格子**：
轉檔判出「這天有上班」但判不出是哪一班（`kind: WORK` 而 `shift: null`）。畫面上不能只寫「上班」兩個字——那等於什麼都沒說。格子印「?」加虛線框、說明寫出原圖那格的原文，並計入待確認數。
_Avoid_: 顯示成一個點、只寫「上班」、靜靜地不計入待確認

**班別推測**：
自動配上的班別標成推測（格子帶 `?`、名單帶「班別推測」），手動指定的標成手動。推不出來就維持「紙本沒寫班別」，不猜。
_Avoid_: 把推測結果當紙本原文呈現、推不出來時隨便填一個班別

**漏坐車風險**：
支援班沒對上班別時，那天可能其實是早班（04:30 到店）或中班，人卻不會出現在交通車名單裡。所以支援班分頁與匯出上車地點都會針對這種情形明確警告，而不是安靜地少一個人。
_Avoid_: 只在班表上標未定卻不提醒車的事、把未定的支援班預設當成不用坐車

**司機版**：
上車名單的另一種輸出，只有日期、車次、站點與人數，不含任何同事姓名，給司機看。店內版才有姓名與當天上班的店。
_Avoid_: 給司機的名單帶姓名、司機版還分店別

**待確認**：
匯出檔的 `needs_review`，意思是轉換器自己也不確定這格判讀對不對，不是事實。班表格畫虛線框，並在卡片與匯入預覽顯示格數。同樣地 `warnings` 會直接點名「某天人數對不上」，一律顯示給使用者。
_Avoid_: 把 needsReview 當成正確資料、把 warnings 只寫進 console

**班別時間**：
早班 04:30-13:00、中班 05:30-14:00、午班 07:30-16:00、晚班 14:00-22:30；D13 晚班是 13:00-21:30。實際以匯入檔帶的時間為準，缺值才用預設。班別可能跨夜（`crosses_midnight`），算重疊時數時下班時間要加一天。
_Avoid_: 全店共用同一組晚班時間、寫死時間而忽略匯入檔、用純 HH:MM 比大小算跨夜班

**崗位**：
主吧、副吧、手沖、輕食、菜口、POS 等；名稱沿用該店匯出檔帶的，統計時以「店別＋崗位」成對計算。**顏色不沿用**——匯出檔的 color 是從 JPEG 量到的實際像素（同一個 POS 黃在 D13 是 `#fffe53`、D7 是 `#fdfd0c`），也沒有深色版本；本站以崗位代碼對到自己的色票。色相沿用紙本的習慣（主吧橘、副吧灰、手沖藍、輕食綠、菜口粉、POS 黃），但任兩個崗位色必須明顯分得出來，有測試把關。
_Avoid_: 全站共用一份崗位主檔、跨店合併同名崗位（未另定時）、拿匯出檔的 color 當設計色票、主吧與 POS 用相近的暖色

**班表看法**：
三種，由左到右：「崗位」（預設，只有底色，畫面最乾淨）、「崗位＋字」（底色再加崗位單字，色盲友善）、「班別」（底色改成班別）。同一份資料三種讀法，不是三套樣式。崗位在三種看法下都會進 `aria-label` 與點開的詳情，切換影響的只是看得見的冗餘。
_Avoid_: 只留一種看法、用小圓點表示崗位、一格裡同時塞班別字＋崗位點＋推測符號＋待確認框、把切換做成只有樣式差異而資料不同

**零值**：
統計表約六成的格子是 0，把 0 印出來只會蓋掉真正的數字。0 一律印成淡點。
_Avoid_: 滿版的 0、用 0 佔位再靠使用者過濾

**顏色不是唯一載體**：
六個崗位色在三種色覺缺陷下無法全部拉到安全距離，所以顏色只負責掃視速度，**崗位字才是正確來源**。崗位色票的明度刻意拉成階梯（L* 70→93）——色覺缺陷破壞色相辨識、不破壞明度，明度階梯是這些色在色盲眼中還分得開的唯一原因。有測試以 Viénot 模擬三種色盲驗證各店實際的崗位組合。
_Avoid_: 只靠顏色表達崗位、把六個色擠在同一個明度帶、只用 RGB 距離判斷「分不分得出來」

**次要資訊的出口**：
班別時間、崗位名稱、支援目的地、推測與待確認的原因，不能只放在 `title`——手機上觸發不了。班表格是可點的按鈕，點了在表格上方顯示同一份說明，並帶 `aria-label`。
_Avoid_: 把唯一的說明放進 tooltip、只在桌機測試就當作有交代

**轉檔資訊**：
判讀提醒、待確認格數與匯出檔備註原文，收在班表下方可收合的「轉檔資訊」裡，不佔用班表上方的版面。
_Avoid_: 把判讀警告常駐在班表上方、把提醒混進班表格子裡

**排班日**：
店長的「排」，不上班，計入休假，也不上交通車。
_Avoid_: 當成一種班別、當成出勤

**交通車**：
早班車（第1班車）與中班車（第2班車）兩台，分開發車也分開算名單；午班與晚班不排交通車。

**發車時間＝第一站的到站時間，一律從站表推**（曾經寫死 04:00／05:00，那其實是最後一站高鐵站的時間，車 03:45 就開了）。**每一站有自己的到站時間，不是一個發車時間配一串站名** —— 兩台車都是每站間隔五分鐘：早班車 03:45／03:50／03:55／04:00，中班車 04:45／04:50／04:55／05:00。司機讀的順序就是他開車的順序，所以司機版照時間排序、沒人的站不列。站名以機捷站號為準（`A21環北站`、`A20興南站`）；`高萱門市`／`高鐵站` 內部用短名，給司機的是 `7-11高萱門市`／`桃園高鐵站`。**改站名一定要在 `PICKUP_LEGACY_ALIASES` 補一筆**，Firebase 存的是舊字串，沒接上那些同事會整批變成「未設定」。
**司機版文字**是直接貼進聊天室傳給司機的，格式照他習慣的寫法：一台車一段、日期底下逐行「時間 站名 幾人」，照時間排序、沒人的站不列。另外三樣是給他自己對帳用的：**日期範圍**（一次傳好幾週才分得出是哪一週）、**星期**、**當日總人數**（接完人數不符就知道漏了誰）。沒人搭的那天寫「停開」——整天消失的話，司機不知道是真的沒班還是訊息被截掉了。
_Avoid_: 一張合併名單、把午班或晚班算進交通車、只標一個發車時間、為了改顯示名稱去動 PICKUP_LOCATIONS 的字串、把沒人的那天直接省略

**依店別篩選**：
三家店的同事混在一份名單裡很難找人。統計表與同事設定都有店別芯片（附人數），所有選人的下拉也依店別分組。跨店的人會出現在他有上班的每一家店底下 —— 找人時記得的是「他在 D7」，不是「他跨店」。
_Avoid_: 一份 27 人的平面下拉、跨店的人只掛在其中一家店

**上車地點**：
A21環北站（03:45）、A20興南站（03:50）、高萱門市（03:55）、高鐵站（04:00），順序就是早班車的行車順序，外加「不搭車」。每人設定一個，早班車與中班車共用；沒設定的人在名單裡列為「未設定」並提示待補。
_Avoid_: 早班與中班各設一個地點（未另定時）、沒設定就當成不搭車

**上車名單**：
依日期列出各上車地點要接的人；範圍可選一週、二週或整月，輸出有文字檔（貼群組）、表格檔 .tsv（貼試算表）與圖檔（同一張表）。
_Avoid_: 只有圖檔、把班表整張當成上車名單

**跨店**：
同一個月出現在一家以上的店（調店或支援）。統計跟著人走，每個班算在實際上班的那家店。
_Avoid_: 依匯出檔來源店計算、把調店的人拆成兩個人

**搭班**：
同一天、同一家店兩人都上班就算搭到班；另外累計班別時間的實際重疊時數。
_Avoid_: 只算同班別才叫搭班、跨店同日也算搭班

**月視圖**：
完整班表篩選到單一同事時的呈現，排版比照 Apple 行事曆（週日起頭、補滿前後月）。跟著人走，跨店與支援班顯示實際上班的店。點某一天會跳回「今天」分頁看那天全店的狀況。
_Avoid_: 只在店別班表裡反白該人的列、月視圖只顯示本店的班

**行事曆匯出**：
把某人的班輸出成 `.ics`：iPhone 點開即可加入行事曆，Google 日曆用「匯入」上傳。時間走本地時間（floating），不綁時區。沒有時間的班（紙本只寫 T3／D7）匯成全天行程，不瞎編時段；休假預設不匯出，可勾選帶入。
_Avoid_: 幫沒寫班別的支援班補一個假時段、把時間硬綁成 UTC、預設把休假也塞進行事曆

**暱稱**：
同事的顯示名稱，覆蓋匯出檔判讀出來的姓名。名單、月視圖、統計與匯出一律顯示暱稱，原名留在括號裡供對照。
_Avoid_: 改掉匯出檔裡的姓名、暱稱只在某些畫面生效

**合併**：
把同一個人的兩個人員鍵收成一個，用在同一個人在不同店被寫成或讀成不同字（例如 D7 的「阿寶」與 D13 的「阿力」）。合併後班表、統計、搭班與行事曆都當成同一個人，隨時可以解除。合併前會檢查：兩邊若在同一天都有班，代表是兩個人，不給合併。
_Avoid_: 自動依相似字合併、合併後無法還原、把不同人併成一人卻沒有檢查

**正式人員鍵**：
合併後留下來的那一個人員鍵，別名都指向它。上車地點、排除統計等設定以正式的那筆為準；合併時若只有別名設過上車地點，會一併帶到正式那筆，免得交通車名單突然少人。
_Avoid_: 合併後兩邊設定各自生效、合併後上車地點默默消失

**目的店的表**：
支援班的班別時間、班別名稱與崗位名稱，一律讀「實際上班的那家店」的代碼表，不能用來源店的（D13 晚班 13:00 起、崗位表各店也不同）。個人月視圖與行事曆匯出尤其要注意，讀錯會匯出錯的上班時間。目的店那個月還沒匯入時，退回該店的預設時間並在畫面與行事曆說明中標示。
_Avoid_: 支援班用來源店的班別時間或崗位表、目的店沒匯入時安靜地用來源店的時間

**週視圖**：
手機上完整班表預設一次顯示一週（週日起頭），可左右翻週；桌機預設整月，兩者可手動切換。週視圖裡不顯示「整月上班／休假」那一欄——那是月統計，放在週視圖裡會誤導，而且剛好讓七天塞不進手機寬度。
_Avoid_: 手機上硬塞 30 欄、週視圖裡混入整月數字、把週切成任意七天而不對齊週日

**行事曆匯出選項**：
店名（同時作為地點欄）、崗位、休假三項各自可開關，並即時預覽事件標題長相。班別一定會有。關掉店名時連 `LOCATION` 欄位一起不寫，而不是寫一個空字串。休假不勾就是行事曆上那幾天留白。
_Avoid_: 匯出內容寫死、關掉的欄位仍寫空值、讓使用者匯出後才發現標題不是想要的

**本店人力**：
當日在這家店上班的人數，定義比照班表 Total 列：上班且**不是**去別店支援。判斷依據是 `isSupport`，**不是 `atStore` 有沒有值** —— 班表有時只寫一個「支」不註明去哪，那種格子 `atStore` 是 null 但人一樣不在本店，用 `atStore != null` 當條件會把他算回來。匯入檔的 `days[].total` 是紙本 Total 列的判讀值，會逐日跟我們算出來的比對；對不上就代表那天一定有格子判錯，那一欄要標出來。
_Avoid_: 把支援出去的人算進本店人力、丟掉 `days[].total`、只把 Total 不符當成一句全域警告

**對帳線索**：
本店人力跟 Total 對不上時，把那一欄 `needsReview` 或信心度低於 0.6 的格子依信心度排出來當線索。實例：D7 2026-05-13 表上 6 人、判讀 7 人，點出 同事 B 信心 9% —— 錯的就在那格。`total_source` 也要看：`auto` 是 Total 列與儲存格互相印證、`cells` 是 Total 列讀不清以儲存格為準、`confirmed` 是人工確認過，三者的可信度不同。
_Avoid_: 只說「這天對不上」卻不給線索、把 `cells` 來源的 Total 當成獨立佐證

**visitor 名字不符**：
目的店備註寫的名字可能跟實際人員對不起來（實例：備註只差一個字，同時像名單上的兩個人）。**不做模糊比對** —— 配錯人的代價是有人漏坐早班車，而這種距離硬猜必錯一半。轉換器比對名單後給 `visitor_candidates`，我們再拿「當天實際說要去那家店的人」取交集，**剛好一個才定案**（來源標 `visitorNarrowed`）；交集不唯一就留白並列出候選。`visitor` 是備註原文，任何情況都不改寫。
_Avoid_: 用編輯距離硬湊名字、名字不符就當作沒有 visitor 而不告訴使用者、把交集後仍多個的情況硬選一個

**confidence 的用法**：
`confidence` 只是同一批判讀內的相對刻度，跨檔比大小沒有意義。只能拿來**排序找線索**（例如 Total 對不上時把那一欄最沒把握的格子排前面），不能設固定門檻當判斷依據。
_Avoid_: `confidence < 0.6` 這種固定門檻、拿不同檔案的 confidence 互相比較

**影子格**：
支援格如果在目的店自己的表上已經有同一個人當天的班（例如一店寫 Alex 支援 D7，而 D7 自己的表上 Alex 上中班），那一格只是「他今天不在本店」的註記，不是另一個班。要標成影子並排除，否則目的店會把同一個人列兩次。以目的店自己那筆為準 —— 它才帶得到班別與崗位。
_Avoid_: 把支援格與目的店自己的紀錄各算一次、拿支援格的資訊蓋掉目的店自己的紀錄

**反推目的地**：
支援格沒寫去哪家店時，看同月哪幾家店「自己的表上」有這個人當天上班，**剛好一家**才採用（標 `destinationFrom: 'ownRecord'`）。兩份獨立紀錄的交集，不是猜的；零家或多家就留白。
_Avoid_: 用最近一次支援的店當預設、多家都有時挑一個

**未命名N**：
轉換器判讀不出姓名時給的 `未命名5`，那個 5 是**這份檔案裡的位置編號**，不是身分。同一家店跨月可以視為同一人（位置穩定），但別家店的「未命名5」是另一個人，所以人員鍵綁店（`未命名5（一店）`），顯示名稱維持原樣。真正的姓名要對原圖確認後用手動合併補。
_Avoid_: 只用姓名當鍵（跨店會把兩個人併成一個）、自作主張猜他是誰

**列外**：
紙本寫了、但**不算在自己店裡**的班——跨店支援與互換班。它們刻意不進當日名單（會重複計人力），但完全看不到就等於資訊掉了，所以另外列出來並標明由哪家店計。互換班兩邊都會列。
_Avoid_: 因為不算人力就整格藏起來、把列外的人也加進當日人數

**自定班別／假別**：
店長可以在轉換器新增班別（`CUSTOM_支_j776`）和假別（`CUSTOM_差_fgeu`），這些代碼是**單店單月**的。跨店畫面查標籤要把範圍內所有月份的 `shift_types`／`leave_types` 合起來查，只拿其中一份會露出原始代碼。時間只在所有店都一致時才顯示——D13 晚班 13:00、其他兩店 14:00，合起來看沒有單一答案，寧可留白。
_Avoid_: 用單一月份文件當跨店字彙、把某一店的班別時間當成通用時間、把宣告了但一格都沒用到的班別畫成 0% 長條

**設定藏起來，待辦露出來**：
同事設定（暱稱、上車地點、合併）設好就很少再動，預設收起，摘要列直接顯示各站人數。但**「未設定 N」大於零時一開始就展開** —— 那代表那天可能少一個人上車。展開狀態只取初值不綁即時判斷，否則最後一個人設好的當下面板會當場收起來，手還在上面。
_Avoid_: 把常用的匯出排在少用的設定後面、用即時條件控制展開狀態

**一個人歸哪家店**：
取範圍內**最後出現的那個月**他排班的店 —— 調店的人看的是他現在在哪，不是他歷史上待過哪些店。一個人只進一組，重複列出來會讓下拉選單出現兩個同名的人、篩選人數也跟總數對不起來。同月出現在兩家店（互換班）就看哪邊班多。
_Avoid_: 用 `storeCodes` membership 直接分組、同一個人在多組重複出現

**搭班頻率**：
比的是搭班傾向，不是相處時間。逐月算「我上班的日子裡有幾成跟他同店」，再對**兩人都在職的月份**取平均。直接比天數的話同期五個月的人一定贏只重疊一個月的人；分母只算共同在職的月份，八月才到職的人才不會為了五到七月被扣分。
重疊 **0 小時不是漏算** —— 中班 14:00 下班、晚班 14:00 上班，同日同店但時間完全不碰；顯示成「交班」而不是 0，否則看起來像壞掉。
兩種看法都要留：**每月平均**答「搭班傾向」，**總時數**答「實際一起站了多久」。同一組資料的兩個問題，排序跟著切換走。
_Avoid_: 用原始搭班天數排序、把對方缺席的月份算進分母、只留其中一種看法

**統計表的欄位**：
只長出「這批人真的有數字」的欄位。事假一年兩天、喪假七天，全部攤成欄位就是十六欄的點點海——**稀有不等於該省略，但也不該跟每天都在用的欄位並排**，所以細項收進「假別」檢視。班別比重長條回答「他是早班的人還是晚班的人」，數字欄回答「幾天」，兩種問題分開答。
_Avoid_: 把所有代碼都攤成欄位、用「顯示全部欄位」當作解法、同一頁放兩個做同一件事的選單

**班別清單的權威來源**：
`shift_types` 就是那家店那個月的完整清單。內建預設（`DEFAULT_SHIFT_TIMES`）只在**檔案已經宣告的代碼缺時間**時補值，絕不拿來補代碼——D13 沒有中班，補回去就會長出一個永遠 0 個班的幽靈欄位，而且它是我們自己生的，追不回任何一張紙本。
_Avoid_: 把預設時間表當成班別清單、`{...defaults, ...fromFile}` 這種寫法

**跨檔對照**：
轉換器的 `--link` 後處理會補目的地（`at_store_source: 'linked'`）、補來訪者姓名（`visitor_match: 'linked'`）、標互換班（`duplicate_of`）。**以匯入檔為準**，但本站自己的推導保留下來當交叉驗證 —— 兩邊算的是同一件事，不一致就代表有一邊錯了，要出警告而不是安靜地選一邊。實測 23 筆全部一致。
_Avoid_: 因為轉換器做了就把自己的推導刪掉、不一致時安靜地採用其中一邊

## 權限

**班表只有管理員能寫**：
`shiftMonths` 與 `shiftSupportLinks` 是 `allow read: if true; allow write: if isAdmin()`。只有店長會匯入班表與改配對，同事只是看；開放寫入等於任何知道 project id 的人都能覆蓋或刪光整個月的班表。上車地點 `shiftPeople` **不鎖**，那是同事自己設的。

**Firestore 規則是 OR，不是覆蓋**：加一條限制規則不會蓋掉放行的規則 —— 底部那條 catch-all 必須把受限的集合逐一 `!=` 排除掉，否則等於沒鎖。這種錯不會有編譯錯誤，只會在你以為鎖好之後繼續裸奔。
_Avoid_: 以為「後面的規則會蓋掉前面的」、把整個 catch-all 改成需要 auth（全站沒有登入機制，那會讓點豆、盤點、叫貨全部寫不進去）

**改規則一定要跑 `npm run test:rules`**：那組測試的重點不是「班表鎖住了嗎」，是**其他功能有沒有被改壞**。需要 Java 與 firebase emulator。

**讀取仍然是開放的**：知道 project id 就讀得到全部班表。這是明知的取捨（全站無登入機制），不是疏漏；要關掉就必須先做登入。
