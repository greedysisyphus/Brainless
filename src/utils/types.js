/**
 * @typedef {Object} ForecastRecord
 * @property {string} date
 * @property {number} passengerCount
 * @property {{ham: number, salami: number}} sandwichCount
 * @property {{ham: number, salami: number}} [ratio]
 */

export const ForecastRecord = {};  // 只是為了導出，實際上不會使用這個物件 